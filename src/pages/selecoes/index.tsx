import { CadastroSelecao, RotaAutenticada } from "components"

//export default CadastroSelecao

export default () => (
    //<SessionProvider>
    <RotaAutenticada>
        <CadastroSelecao />
    </RotaAutenticada>
    //</SessionProvider>
) 